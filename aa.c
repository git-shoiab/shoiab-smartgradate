#include<stdio.h>
int main()
{
	int i=0;

	if(i!=5){//if you give , then the entire thing becomes expressions - left side expression return non zero and right side also returns non zero
        printf("true");
	}
	else{
        printf("false");
	}

	if(printf("hello"));
}
